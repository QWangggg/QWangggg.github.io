## Documented Solutions

`docs/solutions/` contains documented solutions to past problems, organized by category with YAML frontmatter such as `module`, `tags`, and `problem_type`. It is relevant when implementing, debugging, or making decisions in areas that may already have recorded learnings.
